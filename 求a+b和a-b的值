#include<stdio.h>
int main()
{
	int n;
	while(scanf("%d",&n)!=EOF)
	{
		while(n--)
		{
			int a,b;
			scanf("%d%d",&a,&b);
			int count1=0,count2;
			count1=a+b;
			count2=a-b;
			printf("%3d%3d\n",count1,count2);
		}
	}
	return 0;
}
